#include <stdio.h>

int main()
{
	int size;
	//A = 음수를 다 곱한 수
	//B = 음수에서 -1을 빼고 다 곱한 수
	//C = 
	int A = 0, B = 0, C = 0, D = 0, E = 0, F = 0;
	int nums[10001] = { 0, };
	for (int i = 0; i < size; i++)
		scanf("%d", nums[i]);
}
